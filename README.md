# Gulp + webpack initial setup

initial setup for gulp and webpack for front end projects

dependencies:

@babel/core,
@babel/preset-env
babel-loader
browser-sync
gulp
gulp-sass
node-sass
webpack
webpack-stream
